/* eslint-disable import/prefer-default-export */
export { cloneElement, createElement } from './addElement';
export const uniqueId = () => Math.random().toString(16).slice(2);

// export const formMapper = (values) => {

// };
